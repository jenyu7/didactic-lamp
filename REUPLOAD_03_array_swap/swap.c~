//#include <stdio.h>
//#include <stdlib.h>
#include <time.h>

srand(time(NULL));


int arr[10];

for(int i = 0; i < 9; i ++)
  {
    arr[i] = rand();
  }

arr[9] = 0;

for (int i : arr)
  {
    printf("index: %d, value: %d", i, arr[i]);
  }




